<%= form_for obj, :html=>{:class=> 'form form-inline'}, remote:'true' do |f| %>
  <div class='form-group'>
    <%= f.text_area :description, placeholder: 'create a task' %>
    <%= button_tag(type:'submit', class:"btn btn-default btn-xs") do %>
      <span class='glyphicon glyphicon-plus'></span>
    <% end %>
  </div>
<% end %>
