$(function(){
    $('.edit_task_<%=@task.id%>').popover({
    html: true,
    title: 'edit',
    content: '<%= escape_javascript render 'edit_form', task: @task %>',
    placement: 'bottom',
  }).popover('show');
});

