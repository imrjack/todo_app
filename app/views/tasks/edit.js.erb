 $('#popover_<%=@task.id%>').popover({
    html: true,
    url: "tasks/<%=@task.id%>/edit",
    container: 'body',
    placement: 'bottom',
    title: 'title',
    content: '<%= escape_javascript render 'edit_form', task: @task %>'
    }).popover('toggle');
